# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

type Ailment {
  cure: Cure!
  description: String!
  findOne(id: ID, name: String): Ailment!
  id: ID!
  name: String!
}

type Camp {
  id: ID!
  locationId: ID!
  name: String!
  zone: Int!
}

type Cure {
  action: String
  ailmentId: Int!
  items: [Item!]!
  protections: [Skill!]!
}

type Item {
  carryLimit: Int!
  description: String!
  findOne(id: ID, name: String): Item!
  id: ID!
  name: String!
  rarity: Int!
  value: Int!
}

type Location {
  camps: [Camp!]!
  findOne(id: ID, name: String): Location!
  id: ID!
  name: String!
  zoneCount: Int!
}

"""
The Modifier scalar is a JsonObject that describes the stat modifiers granted by a skill rank
"""
scalar Modifier

type Query {
  """Find one ailment by id or name.If both is given, id will be used."""
  ailment(id: ID, name: String): Ailment
  ailments(after: ID, before: ID, limit: Int = 25, name: String): [Ailment!]!

  """Find one item by id or name.If both is given, id will be used."""
  item(id: ID, name: String): Item
  items(after: ID, before: ID, limit: Int = 25, name: String): [Item!]!

  """Find one location by id or name.If both is given, id will be used."""
  location(id: ID, name: String): Location
  locations(after: ID, before: ID, limit: Int = 25, name: String): [Location!]!

  """Find one skill by id or name.If both is given, id will be used."""
  skill(id: ID, name: String): Skill
  skills(after: ID, before: ID, limit: Int = 25, name: String): [Skill!]!
}

type Skill {
  description: String!
  findOne(id: ID, name: String): Skill!
  id: ID!
  name: String!
  ranks: [SkillRank!]!
}

type SkillRank {
  description: String!
  id: ID!
  level: Int!
  modifiers: Modifier
  skillId: ID!
  skillName: String!
}
